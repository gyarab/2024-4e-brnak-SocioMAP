<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SocioMap" parent="Theme.Material3.Light.NoActionBar">

        <!-- Background Colors -->
        <item name="android:windowBackground">@color/default_background_light</item>
        <item name="colorBackground">@color/default_background_light</item>
        <item name="colorOnBackground">@color/white</item>
        <item name="colorOnSurface">@color/text_primary_light</item>

        <!-- Primary & Accent Colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorAccent">@color/filter_button</item>

        <!-- System Bars -->
        <item name="android:statusBarColor">@color/status_bar_light</item>
        <item name="android:navigationBarColor">@color/nav_bar_light</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Button Style
        <item name="android:buttonStyle">@style/ButtonStyle</item>
        -->
    </style>
</resources>